/**
 * Maps pathname segments to human-readable breadcrumb labels.
 * Add new entries here to keep titles centralized.
 */
export const BREADCRUMBS_MAP: Record<string, string> = {
  '/panel': 'Panel de Control',
  '/panel/panel': 'Panel Principal',
};
